<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        {% block stylesheets %}
        <style>
            body {
                background-color: #e8f4f8;
                font-family: 'Arial', sans-serif;
            }
            h1 {
                color: #2c3e50;
                font-weight: bold;
                text-transform: uppercase;
                margin-bottom: 30px;
            }
        </style>
        {% endblock %}

        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        {% endblock %}
    </head>
    <body>
        <nav class="navbar bg-info">
            <div class="container-fluid">
                <a class="navbar-brand">Liste des Montages</a>
                <div class="d-flex">
                    <a class="btn btn-success me-2" href="{{ path('app_montage_index') }}">Gestion Montages</a>
                    <a class="btn btn-success" href="{{ path('app_piece_index') }}">Gestion Pièces</a>
                </div>
            </div>
        </nav>

        <div class="container mt-4">
            {% block body %}{% endblock %}
        </div>
    </body>
</html>
