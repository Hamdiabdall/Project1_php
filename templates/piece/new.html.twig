{% extends 'base.html.twig' %}

{% block title %}Nouvelle Pièce{% endblock %}

{% block body %}
    <div class="container">
        <h1>Créer une nouvelle pièce</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
        {{ form_end(form) }}

        <a href="{{ path('app_piece_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    </div>
{% endblock %} 